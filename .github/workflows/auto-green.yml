name: ci

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *" # 매일 한국 시간 기준 오전 9시에 자동 실행

jobs:
  autogreen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Auto green
        run: |
          git config --local user.email "kylechoi4200@gmail.com"
          git config --local user.name "Crypto_paperplane"
          git commit --allow-empty -m "docs: update log"
          git push
